# Model card — Raman Spectra Classifier

**Model**: 3SSE: PCA + XGBoost → Ensemble (top-3)
**Development data**: D:/BARC/Data
**Validation**: 5-fold patient-grouped CV (nested hyperparameter tuning inside folds)

## Preprocessing
```
crop_min = 500.0
crop_max = 2000.0
despike = False
despike_z = 7.0
despike_window = 5
wavelet = True
wavelet_name = sym8
wavelet_level = 4
wavelet_threshold = universal
wavelet_mode = soft
wavelet_cycle = 0
sg_window = 7
sg_poly = 5
sg_deriv = 0
detrend = True
baseline_method = als
als_lambda = 100000.0
als_p = 0.01
als_niter = 10
norm = vector
wn_calibrate = True
```

## Performance (out-of-fold, pooled)
- macro-F1: **0.839**
- ROC AUC: **0.881** · AUPRC: **0.925**
- AUC power: detectable AUC at 80% power = 0.556 (n+ 160 / n− 127; ~25 per group needed to reach the OBSERVED AUC 0.88 at 80% power)

## Limitations
- Single-center development; no external cohort validation yet (TRIPOD+AI gap).
- Spectrum-level CV numbers can exceed patient-level reality; patient-grouped CV is the reported standard.
- Triage-to-biopsy aid, not a diagnosis.
- Conformal sets abstain on ambiguous spectra; coverage is marginal (~90%), not per-patient.

## Honest performance context (2026-09-06)
- **Honest (nested) macro-F1: 0.564 ± 0.045** (session log, 2026-09-06
  11:58) — the 0.839 above is selection-optimistic (winner chosen on the
  same CV that scores it). Expect the nested estimate on unseen patients.
- McNemar vs the paired PCA+SVM baseline: p = 0.629 (improvement NOT
  significant — `study_run_3sse/significance.json`).
- Per-class CV sens/spec were not persisted when this card was written
  (fixed since: bundles now store `per_class`).

## Deploy-path fix verification (2026-09-06)
A train/deploy mismatch was found and fixed: bundles trained with
`wn_calibrate = True` were predicted WITHOUT the Phe-1003 alignment, so
this model received shifted, out-of-distribution features at predict
time and called EVERY spectrum Tumor (p 0.94–0.97, including all normal
patients). After the fix (no retraining — the bundle is unchanged),
re-prediction over the full development tree (341 files, incl.
hygiene-excluded ones; 5 tumor spectra skipped for missing normal
reference):

| level | sens | spec | acc |
|---|---|---|---|
| spectrum (n=336 predicted) | 0.749 | 0.941 | 0.836 |
| patient rollup (any site ≥ 0.750 calibrated) | 58/74 tumor-site patients detected | 5/75 patients with a normal site flagged | — |

Remaining imperfection reflects the model's honest quality (nested F1
≈ 0.56), not the deploy path. Verification script:
`raman_app/verify_deploy_fix.py`.

_Auto-generated by the Raman Spectra Classifier._
